Add-Type -AssemblyName System.Windows.Forms
Add-Type -AssemblyName System.Drawing

# Importar funciones de Windows API
Add-Type @"
using System;
using System.Runtime.InteropServices;
public class NativeMethods {
    [DllImport("user32.dll")]
    public static extern void mouse_event(int dwFlags, int dx, int dy, int dwData, int dwExtraInfo);
    
    [DllImport("user32.dll")]
    public static extern short GetAsyncKeyState(int vKey);
    
    public const int MOUSEEVENTF_LEFTDOWN = 0x02;
    public const int MOUSEEVENTF_LEFTUP = 0x04;
    public const int MOUSEEVENTF_RIGHTDOWN = 0x08;
    public const int MOUSEEVENTF_RIGHTUP = 0x10;
}
"@

# Variables globales
$script:leftClickTimer = New-Object System.Windows.Forms.Timer
$script:rightClickTimer = New-Object System.Windows.Forms.Timer
$script:keyMonitorTimer = New-Object System.Windows.Forms.Timer
$script:leftHotkey = 0x00
$script:rightHotkey = 0x00
$script:leftEnabled = $false
$script:rightEnabled = $false
$script:leftPreviousState = $false
$script:rightPreviousState = $false
$script:leftListeningMode = $false
$script:rightListeningMode = $false

# Función para obtener el nombre de la tecla
function Get-KeyName {
    param([int]$keyCode)
    
    $keyNames = @{
        0x01 = "LMB"
        0x02 = "RMB"
        0x04 = "MMB"
        0x05 = "MB4"
        0x06 = "MB5"
        0x20 = "Space"
        0x10 = "Shift"
        0x11 = "Ctrl"
        0x12 = "Alt"
        0x41 = "A"; 0x42 = "B"; 0x43 = "C"; 0x44 = "D"; 0x45 = "E"
        0x46 = "F"; 0x47 = "G"; 0x48 = "H"; 0x49 = "I"; 0x4A = "J"
        0x4B = "K"; 0x4C = "L"; 0x4D = "M"; 0x4E = "N"; 0x4F = "O"
        0x50 = "P"; 0x51 = "Q"; 0x52 = "R"; 0x53 = "S"; 0x54 = "T"
        0x55 = "U"; 0x56 = "V"; 0x57 = "W"; 0x58 = "X"; 0x59 = "Y"
        0x5A = "Z"
        0x70 = "F1"; 0x71 = "F2"; 0x72 = "F3"; 0x73 = "F4"
        0x74 = "F5"; 0x75 = "F6"; 0x76 = "F7"; 0x77 = "F8"
    }
    
    if ($keyNames.ContainsKey($keyCode)) {
        return $keyNames[$keyCode]
    }
    return "Key"
}

# Función para capturar tecla presionada
function Get-PressedKey {
    for ($i = 0x01; $i -le 0xFE; $i++) {
        $state = [NativeMethods]::GetAsyncKeyState($i)
        if (($state -band 0x8000) -ne 0) {
            return $i
        }
    }
    return 0
}

# Funciones de click
function Invoke-LeftClick {
    [NativeMethods]::mouse_event([NativeMethods]::MOUSEEVENTF_LEFTDOWN, 0, 0, 0, 0)
    [NativeMethods]::mouse_event([NativeMethods]::MOUSEEVENTF_LEFTUP, 0, 0, 0, 0)
}

function Invoke-RightClick {
    [NativeMethods]::mouse_event([NativeMethods]::MOUSEEVENTF_RIGHTDOWN, 0, 0, 0, 0)
    [NativeMethods]::mouse_event([NativeMethods]::MOUSEEVENTF_RIGHTUP, 0, 0, 0, 0)
}

# Monitor de teclas
function Update-KeyStates {
    # Monitor click izquierdo
    if ($script:leftEnabled -and $script:leftHotkey -ne 0x00) {
        $keyState = [NativeMethods]::GetAsyncKeyState($script:leftHotkey)
        $isPressed = ($keyState -band 0x8000) -ne 0
        
        if ($isPressed -and -not $script:leftPreviousState) {
            $script:leftClickTimer.Start()
        } elseif (-not $isPressed -and $script:leftPreviousState) {
            $script:leftClickTimer.Stop()
        }
        
        $script:leftPreviousState = $isPressed
    }
    
    # Monitor click derecho
    if ($script:rightEnabled -and $script:rightHotkey -ne 0x00) {
        $keyState = [NativeMethods]::GetAsyncKeyState($script:rightHotkey)
        $isPressed = ($keyState -band 0x8000) -ne 0
        
        if ($isPressed -and -not $script:rightPreviousState) {
            $script:rightClickTimer.Start()
        } elseif (-not $isPressed -and $script:rightPreviousState) {
            $script:rightClickTimer.Stop()
        }
        
        $script:rightPreviousState = $isPressed
    }
    
    # Modo escucha para tecla izquierda
    if ($script:leftListeningMode) {
        $pressedKey = Get-PressedKey
        if ($pressedKey -ne 0) {
            $script:leftHotkey = $pressedKey
            $keyName = Get-KeyName $pressedKey
            $leftKeyLabel.Text = $keyName
            $leftKeyLabel.ForeColor = [System.Drawing.Color]::White
            $script:leftListeningMode = $false
            $script:leftEnabled = $true
            Start-Sleep -Milliseconds 200
        }
    }
    
    # Modo escucha para tecla derecha
    if ($script:rightListeningMode) {
        $pressedKey = Get-PressedKey
        if ($pressedKey -ne 0) {
            $script:rightHotkey = $pressedKey
            $keyName = Get-KeyName $pressedKey
            $rightKeyLabel.Text = $keyName
            $rightKeyLabel.ForeColor = [System.Drawing.Color]::White
            $script:rightListeningMode = $false
            $script:rightEnabled = $true
            Start-Sleep -Milliseconds 200
        }
    }
}

# Crear formulario
$form = New-Object System.Windows.Forms.Form
$form.Text = ""
$form.Size = New-Object System.Drawing.Size(340, 250)
$form.StartPosition = "CenterScreen"
$form.FormBorderStyle = "None"
$form.BackColor = [System.Drawing.Color]::FromArgb(20, 20, 20)
$form.Font = New-Object System.Drawing.Font("Segoe UI", 9)

# Hacer el formulario arrastrable
$script:dragging = $false
$script:dragCursor = [System.Drawing.Point]::Empty
$script:dragForm = [System.Drawing.Point]::Empty

$form.Add_MouseDown({
    if ($_.Button -eq [System.Windows.Forms.MouseButtons]::Left -and $_.Y -lt 45) {
        $script:dragging = $true
        $script:dragCursor = [System.Windows.Forms.Cursor]::Position
        $script:dragForm = $form.Location
    }
})

$form.Add_MouseMove({
    if ($script:dragging) {
        $diff = [System.Drawing.Point]::new(
            [System.Windows.Forms.Cursor]::Position.X - $script:dragCursor.X,
            [System.Windows.Forms.Cursor]::Position.Y - $script:dragCursor.Y
        )
        $form.Location = [System.Drawing.Point]::new(
            $script:dragForm.X + $diff.X,
            $script:dragForm.Y + $diff.Y
        )
    }
})

$form.Add_MouseUp({
    if ($_.Button -eq [System.Windows.Forms.MouseButtons]::Left) {
        $script:dragging = $false
    }
})

# Header Panel
$headerPanel = New-Object System.Windows.Forms.Panel
$headerPanel.Location = New-Object System.Drawing.Point(0, 0)
$headerPanel.Size = New-Object System.Drawing.Size(340, 45)
$headerPanel.BackColor = [System.Drawing.Color]::FromArgb(20, 20, 20)
$form.Controls.Add($headerPanel)

# Título
$titleLabel = New-Object System.Windows.Forms.Label
$titleLabel.Location = New-Object System.Drawing.Point(15, 10)
$titleLabel.Size = New-Object System.Drawing.Size(250, 25)
$titleLabel.Text = "Icon1cClicker"
$titleLabel.Font = New-Object System.Drawing.Font("Segoe UI", 13, [System.Drawing.FontStyle]::Bold)
$titleLabel.ForeColor = [System.Drawing.Color]::White
$headerPanel.Controls.Add($titleLabel)

# Botón cerrar
$closeBtn = New-Object System.Windows.Forms.Label
$closeBtn.Location = New-Object System.Drawing.Point(305, 8)
$closeBtn.Size = New-Object System.Drawing.Size(25, 25)
$closeBtn.Text = "×"
$closeBtn.ForeColor = [System.Drawing.Color]::FromArgb(180, 180, 180)
$closeBtn.Font = New-Object System.Drawing.Font("Segoe UI", 16)
$closeBtn.TextAlign = "MiddleCenter"
$closeBtn.Cursor = [System.Windows.Forms.Cursors]::Hand
$headerPanel.Controls.Add($closeBtn)

# Left Click Section
$leftPanel = New-Object System.Windows.Forms.Panel
$leftPanel.Location = New-Object System.Drawing.Point(15, 60)
$leftPanel.Size = New-Object System.Drawing.Size(310, 60)
$leftPanel.BackColor = [System.Drawing.Color]::FromArgb(25, 25, 25)
$form.Controls.Add($leftPanel)

$leftLabel = New-Object System.Windows.Forms.Label
$leftLabel.Location = New-Object System.Drawing.Point(10, 8)
$leftLabel.Size = New-Object System.Drawing.Size(70, 20)
$leftLabel.Text = "Left click"
$leftLabel.ForeColor = [System.Drawing.Color]::FromArgb(150, 150, 150)
$leftLabel.Font = New-Object System.Drawing.Font("Segoe UI", 9)
$leftPanel.Controls.Add($leftLabel)

$leftKeyLabel = New-Object System.Windows.Forms.Label
$leftKeyLabel.Location = New-Object System.Drawing.Point(90, 5)
$leftKeyLabel.Size = New-Object System.Drawing.Size(70, 26)
$leftKeyLabel.Text = "none"
$leftKeyLabel.ForeColor = [System.Drawing.Color]::FromArgb(120, 120, 120)
$leftKeyLabel.Font = New-Object System.Drawing.Font("Segoe UI", 10)
$leftKeyLabel.Cursor = [System.Windows.Forms.Cursors]::Hand
$leftKeyLabel.BorderStyle = "FixedSingle"
$leftKeyLabel.BackColor = [System.Drawing.Color]::FromArgb(30, 30, 30)
$leftKeyLabel.TextAlign = "MiddleCenter"
$leftPanel.Controls.Add($leftKeyLabel)

$leftCpsValueLabel = New-Object System.Windows.Forms.Label
$leftCpsValueLabel.Location = New-Object System.Drawing.Point(260, 8)
$leftCpsValueLabel.Size = New-Object System.Drawing.Size(40, 20)
$leftCpsValueLabel.Text = "10"
$leftCpsValueLabel.ForeColor = [System.Drawing.Color]::White
$leftCpsValueLabel.Font = New-Object System.Drawing.Font("Segoe UI", 10, [System.Drawing.FontStyle]::Bold)
$leftCpsValueLabel.TextAlign = "MiddleRight"
$leftPanel.Controls.Add($leftCpsValueLabel)

$leftSlider = New-Object System.Windows.Forms.TrackBar
$leftSlider.Location = New-Object System.Drawing.Point(10, 32)
$leftSlider.Size = New-Object System.Drawing.Size(290, 25)
$leftSlider.Minimum = 1
$leftSlider.Maximum = 25
$leftSlider.Value = 10
$leftSlider.TickStyle = "None"
$leftSlider.BackColor = [System.Drawing.Color]::FromArgb(25, 25, 25)
$leftPanel.Controls.Add($leftSlider)

# Right Click Section
$rightPanel = New-Object System.Windows.Forms.Panel
$rightPanel.Location = New-Object System.Drawing.Point(15, 130)
$rightPanel.Size = New-Object System.Drawing.Size(310, 60)
$rightPanel.BackColor = [System.Drawing.Color]::FromArgb(25, 25, 25)
$form.Controls.Add($rightPanel)

$rightLabel = New-Object System.Windows.Forms.Label
$rightLabel.Location = New-Object System.Drawing.Point(10, 8)
$rightLabel.Size = New-Object System.Drawing.Size(70, 20)
$rightLabel.Text = "Right click"
$rightLabel.ForeColor = [System.Drawing.Color]::FromArgb(150, 150, 150)
$rightLabel.Font = New-Object System.Drawing.Font("Segoe UI", 9)
$rightPanel.Controls.Add($rightLabel)

$rightKeyLabel = New-Object System.Windows.Forms.Label
$rightKeyLabel.Location = New-Object System.Drawing.Point(90, 5)
$rightKeyLabel.Size = New-Object System.Drawing.Size(70, 26)
$rightKeyLabel.Text = "none"
$rightKeyLabel.ForeColor = [System.Drawing.Color]::FromArgb(120, 120, 120)
$rightKeyLabel.Font = New-Object System.Drawing.Font("Segoe UI", 10)
$rightKeyLabel.Cursor = [System.Windows.Forms.Cursors]::Hand
$rightKeyLabel.BorderStyle = "FixedSingle"
$rightKeyLabel.BackColor = [System.Drawing.Color]::FromArgb(30, 30, 30)
$rightKeyLabel.TextAlign = "MiddleCenter"
$rightPanel.Controls.Add($rightKeyLabel)

$rightCpsValueLabel = New-Object System.Windows.Forms.Label
$rightCpsValueLabel.Location = New-Object System.Drawing.Point(260, 8)
$rightCpsValueLabel.Size = New-Object System.Drawing.Size(40, 20)
$rightCpsValueLabel.Text = "10"
$rightCpsValueLabel.ForeColor = [System.Drawing.Color]::White
$rightCpsValueLabel.Font = New-Object System.Drawing.Font("Segoe UI", 10, [System.Drawing.FontStyle]::Bold)
$rightCpsValueLabel.TextAlign = "MiddleRight"
$rightPanel.Controls.Add($rightCpsValueLabel)

$rightSlider = New-Object System.Windows.Forms.TrackBar
$rightSlider.Location = New-Object System.Drawing.Point(10, 32)
$rightSlider.Size = New-Object System.Drawing.Size(290, 25)
$rightSlider.Minimum = 1
$rightSlider.Maximum = 25
$rightSlider.Value = 10
$rightSlider.TickStyle = "None"
$rightSlider.BackColor = [System.Drawing.Color]::FromArgb(25, 25, 25)
$rightPanel.Controls.Add($rightSlider)

# Self Destruct Button
$selfDestructBtn = New-Object System.Windows.Forms.Button
$selfDestructBtn.Location = New-Object System.Drawing.Point(15, 205)
$selfDestructBtn.Size = New-Object System.Drawing.Size(310, 32)
$selfDestructBtn.Text = "SELF DESTRUCT"
$selfDestructBtn.BackColor = [System.Drawing.Color]::FromArgb(120, 20, 20)
$selfDestructBtn.ForeColor = [System.Drawing.Color]::White
$selfDestructBtn.FlatStyle = "Flat"
$selfDestructBtn.FlatAppearance.BorderSize = 0
$selfDestructBtn.Font = New-Object System.Drawing.Font("Segoe UI", 9, [System.Drawing.FontStyle]::Bold)
$selfDestructBtn.Cursor = [System.Windows.Forms.Cursors]::Hand
$form.Controls.Add($selfDestructBtn)

# Event Handlers
$leftKeyLabel.Add_Click({
    $leftKeyLabel.Text = "..."
    $leftKeyLabel.ForeColor = [System.Drawing.Color]::FromArgb(220, 80, 80)
    $script:leftListeningMode = $true
})

$rightKeyLabel.Add_Click({
    $rightKeyLabel.Text = "..."
    $rightKeyLabel.ForeColor = [System.Drawing.Color]::FromArgb(220, 80, 80)
    $script:rightListeningMode = $true
})

$leftSlider.Add_ValueChanged({
    $leftCpsValueLabel.Text = $leftSlider.Value.ToString()
    $interval = 1000.0 / [double]$leftSlider.Value
    $script:leftClickTimer.Interval = [Math]::Max(1, [int]$interval)
})

$rightSlider.Add_ValueChanged({
    $rightCpsValueLabel.Text = $rightSlider.Value.ToString()
    $interval = 1000.0 / [double]$rightSlider.Value
    $script:rightClickTimer.Interval = [Math]::Max(1, [int]$interval)
})

$closeBtn.Add_Click({
    $form.Close()
})

$closeBtn.Add_MouseEnter({
    $closeBtn.ForeColor = [System.Drawing.Color]::FromArgb(220, 80, 80)
})

$closeBtn.Add_MouseLeave({
    $closeBtn.ForeColor = [System.Drawing.Color]::FromArgb(180, 180, 180)
})

$selfDestructBtn.Add_Click({
    $result = [System.Windows.Forms.MessageBox]::Show(
        "Close everything?",
        "Self Destruct",
        [System.Windows.Forms.MessageBoxButtons]::YesNo,
        [System.Windows.Forms.MessageBoxIcon]::Warning
    )
    
    if ($result -eq [System.Windows.Forms.DialogResult]::Yes) {
        $selfDestructBtn.Text = "DESTRUCTING..."
        $selfDestructBtn.BackColor = [System.Drawing.Color]::FromArgb(180, 20, 20)
        $form.Refresh()
        Start-Sleep -Milliseconds 300
        
        $script:leftClickTimer.Stop()
        $script:rightClickTimer.Stop()
        $script:keyMonitorTimer.Stop()
        
        try {
            Get-Process | Where-Object { $_.ProcessName -eq "powershell" -or $_.ProcessName -eq "pwsh" } | ForEach-Object {
                if ($_.Id -ne $PID) {
                    Stop-Process -Id $_.Id -Force -ErrorAction SilentlyContinue
                }
            }
            
            Get-Process | Where-Object { $_.ProcessName -eq "cmd" } | ForEach-Object {
                Stop-Process -Id $_.Id -Force -ErrorAction SilentlyContinue
            }
        } catch {}
        
        $form.Close()
        Stop-Process -Id $PID -Force
    }
})

# Configurar timers
$script:leftClickTimer.Add_Tick({ Invoke-LeftClick })
$script:rightClickTimer.Add_Tick({ Invoke-RightClick })
$script:leftClickTimer.Interval = 100
$script:rightClickTimer.Interval = 100

$script:keyMonitorTimer.Interval = 10
$script:keyMonitorTimer.Add_Tick({ Update-KeyStates })
$script:keyMonitorTimer.Start()

$form.Add_FormClosing({
    $script:leftClickTimer.Stop()
    $script:rightClickTimer.Stop()
    $script:keyMonitorTimer.Stop()
    $script:leftClickTimer.Dispose()
    $script:rightClickTimer.Dispose()
    $script:keyMonitorTimer.Dispose()
})

$form.Add_Shown({$form.Activate()})
[void]$form.ShowDialog()
